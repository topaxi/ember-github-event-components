import { gravatar } from 'dummy/helpers/gravatar';
import { module, test } from 'qunit';

module('Unit | Helper | gravatar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gravatar([42]);
  assert.ok(result);
});
